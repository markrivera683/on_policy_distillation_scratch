[project]
name = "on-policy-distillation-scratch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dataclasses>=0.8",
    "torch>=2.12.0",
    "transformers>=5.9.0",
    "typing>=3.10.0.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
